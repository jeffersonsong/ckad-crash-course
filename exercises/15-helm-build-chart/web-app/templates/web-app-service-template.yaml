apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-world
  name: web-app-service
spec:
  ports:
  - port: {{ .Values.service_port }}
    protocol: TCP
    targetPort: {{ .Values.container_port }}
  selector:
    app: hello-world
  type: ClusterIP
